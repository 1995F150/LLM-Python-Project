[Unit]
Description=CriderGPT Engine Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/cridergpt-engine
ExecStart=/opt/cridergpt-engine/venv/bin/uvicorn app:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
