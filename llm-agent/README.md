# Legacy compatibility directory

The original experimental agent was duplicated and syntactically invalid. These files now forward older imports to the production modules at the repository root. New code should use `app.py`, `engine/`, and `memory/` directly.

